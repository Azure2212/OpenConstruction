# data/samples — SYNTHETIC fixtures (provenance log)

**ALL files here are SYNTHETIC / REDUCED stand-ins generated deterministically by
`workingGroup/specialists/OC_DATA_1/outputs/gen_category_C_fixtures.py` — they are NOT real
dataset content and must never be presented as such.** Purpose: ground-truth fixtures for the
RETRIEVE benchmark (Category C: file-inventory + format-detection) at $0 with no downloads.
Each dir mirrors the *shape* of a `sample-corpus-spec.json` entry (point cloud / image+annotation /
tabular). `_FIXTURE.json` per dir holds the GT inventory. Real file inventories = live retrieve (Phase 3+).
