{
  "_comment": "Copy to .llm-config.json (gitignored). LMStudio local validated: baseUrl includes /v1.",
  "_comment_embed": "embedModel = the EMBEDDING model for the RAG-dense baseline (scripts/run_dense_rag.js). Must be an embedding model, NOT the chat `model` above. LM Studio: load 'text-embedding-nomic' (or similar) and serve it at the same baseUrl/v1/embeddings.",
  "baseUrl": "http://localhost:19234/v1",
  "model": "qwen/qwen3.6-35b-a3b",
  "embedModel": "text-embedding-nomic",
  "temperature": 0,
  "max_tokens": 1500,
  "apiKey": "",
  "timeout_ms": 120000
}
